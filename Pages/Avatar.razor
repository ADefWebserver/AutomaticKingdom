@inject IJSRuntime JSRuntime

<BabylonAvatar ID="babylon5" PrepareStage="prepareStage" PrepareScene="prepareScene" style="width: 80%;height: 500px;">
</BabylonAvatar>

@code {

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            string ElementID = "babylon5";
            string FileName = "dummy3.babylon.json";
            string MeshName = "YBot";

            await AutomaticKingdomInterop.LoadMesh(
                JSRuntime,
                ElementID,
                FileName,
                MeshName
                );
        }
    }
}